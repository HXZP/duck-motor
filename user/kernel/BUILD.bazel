load("//bazel/mcu:mcu_rules.bzl", "mcu_library")

package(default_visibility = ["//visibility:public"])

mcu_library(
    name = "user_kernel",
    srcs = ["pt-thread.c"],
    hdrs = glob(["*.h"]),
    includes = ["user"],
)
